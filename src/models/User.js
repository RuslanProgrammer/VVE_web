export default class User {
    constructor(email, password, name, surname, shop) {
        this.name = name;
        this.surname = surname;
        this.shop = shop;
        this.email = email;
        this.password = password;
    }
}
